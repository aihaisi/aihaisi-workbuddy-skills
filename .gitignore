# ── 第三方技能（外部来源，不纳入本仓库） ──────────────────────────
# ginger_wechat_portrait：上游 Jiang59991/ginger_wechat_portrait，
# 无 LICENSE（默认保留全部权利），且自带 .git。本仓库只登记来源，不转发其代码。
# 重新安装：git clone https://github.com/Jiang59991/ginger_wechat_portrait.git
ginger_wechat_portrait/

# ── Python 编译产物 ───────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd

# ── 安装器状态（会泄漏本机路径，且被自动重写） ──────────────────────
# skill_2053084036212973568 是从 marketplace 安装的技能，其 _skillhub_meta.json
# 含 iconLocalPath 字段 = 本机绝对路径 C:\Users\<用户名>\...
# 该字段由 WorkBuddy 每次扫描技能时重新生成 —— 手工脱敏后约 1 分钟即被写回，
# 因此「改一次」无效，只能不跟踪（磁盘文件保留，功能不受影响）。
skill_2053084036212973568/_skillhub_meta.json

# ── 运行时数据 / 临时文件 ─────────────────────────────────────────
*.log
*.tmp
.DS_Store
Thumbs.db
