> 新供应商快速筛选 SKILL · 企查查 MCP V2.0 增强版。
> 招投标或采购寻源阶段对候选供应商的批量快速筛选工具。与"供应商准入评估"的深度核验不同，本 SKILL 聚焦"快速筛选 + 去伪存真"——一次扫描多家候选供应商，输出排序后的短名单。V2.0 新增双随机抽查记录核对。
>
> 核心能力：
> - 批量扫描候选供应商（每次可处理 5-20 家）
> - 9 项核心红线快筛（失信 / 限高 / 被执行 / 经营异常 / 破产 / 资不抵债 / 重大处罚 / 股权冻结 / 吊销）
> - **V2.0 新能力**：`mcp__qcc-operation__get_random_check` 双随机抽查记录核对——只展示已完成记录，不推断结果、不参与评分
> - 输出短名单（排除红线 + 按综合评分排序）
>
> 适用场景：招标寻源阶段候选供应商排查 / 集中采购前的候选池筛选 / 新业务领域供应商批量扫描。
>
> 使用方式：/new-supplier-screening 供应商 1 / 供应商 2 / ... [--top N 返回前 N 名] [--format md|docx|pptx]
>
> **风险核查采用「先扫后钻」**：先通过企业风险全量扫描一次性分诊 35 项风险维度、快速定位命中项，再对命中维度深入取证——既不漏维度，也避免逐项无效查询。

**命令**：`/new-supplier-screening` · **MCP 工具集**：`qcc-company, qcc-risk, qcc-operation`

---

## MCP Resource 条件读取（跨客户端兼容）

> 本 SKILL 的 Resource 均为 `auto_read_allowed=false`，不得假设连接时自动加载或跨会话保留。

1. 新会话首次执行时，通过标准 `resources/list` 发现并用 `resources/read` 读取核心术语、数据纪律、实体锚定和 `qcc://skill/new-supplier-screening/tool-binding`。
2. 最终报告生成前读取 `qcc://skill/new-supplier-screening/report-template`。评分权重和排除阈值仍由本 SKILL 内联规则兜底，未单独迁为领域标准。
3. 动态工具字典只按当前 Server、按需读取，禁止一次读取六个 Server 或据此全量调用工具。
4. 同会话同版本不重复读取；新会话重新发现。客户端不支持、未授权或读取失败时不循环重试，继续使用 A 层与本 SKILL 内联内容。
5. Resource URI、读取动作和内部工具代码不得写入客户报告。

---

## 🔍 风险维度扫描 · 先扫后钻（统一规范 · 2026-06-08 · 对齐 A 层铁律 5-A）

> 本 SKILL 凡涉及“一次性排查 ≥ 2 个企业风险维度”（司法风险 / 失信 / 被执行 / 限高 / 经营异常 / 行政处罚 / 破产 / 担保 / 税务 等 qcc-risk 维度），**一律按“先扫后钻”执行，禁止逐个原子风险工具散弹枪式调用**（慢 / 贵 / 多为无效调用）：
>
> 1. **第 1 步 · 分诊（先扫）**：先调 `mcp__qcc-risk__get_company_risk_scan`（企业风险扫描）一次返回企业**自身** 35 项风险维度的命中计数（脱水版：有 / 无 + 条数，不含明细）。
> 2. **第 2 步 · 下钻（后钻）**：仅对 `count > 0` 的维度，调对应原子风险工具取明细（具体工具见本 SKILL 工作流 / 术语对照表）。示例：scan 显示「失信 2、被执行 1、其余 0」→ 只下钻 `mcp__qcc-risk__get_dishonest_info` + `mcp__qcc-risk__get_judgment_debtor_info`。
> 3. **`count = 0` 的维度**：直接判定“无记录”，不再调用该维度原子工具。
> 4. **明确单一维度问句**（仅查某一项，如“有没有失信”）→ 直接调对应原子工具，无需先扫（对应 A 层铁律 5-A 路由 3）。
> 5. scan 只分诊、不出明细；要明细必须下钻原子工具。风险结论只陈述“命中维度 + 计数 / 明细”客观事实，**不替客户判定“能不能合作 / 可不可开户”**。
> 6. 先扫后钻发生在**实体锚定确定唯一主体之后**；简称 / 品牌名仍须先 `mcp__qcc-company__get_company_by_query` 锁定主体，再 scan。
> 7. 可引用已上线的聚合风险扫描工具：`mcp__qcc-risk__get_company_risk_scan`（企业自身）、`mcp__qcc-executive__get_executive_risk_scan`（董监高个人）、`mcp__qcc-risk__get_company_related_risk_scan`（企业关联）、`mcp__qcc-executive__get_executive_related_risk_scan`（人关联）；关联扫描遵守**单层预警 · 禁自动下钻**；仍不得引用任何尚未上线的工具。
>
> 8. **【定性必须有下钻证据】** 对任一风险维度给出**定性判断**（如“多为原告身份 / 属正常维权”“轻微合规瑕疵”“诉讼活跃度正常”等）之前，必须已下钻该维度的明细工具、拿到支撑数据；未下钻则**只陈述 scan 计数并标注“（未取明细）”**，禁止凭 scan 计数或印象给定性。例：scan 显示「裁判文书 77」但未下钻 `mcp__qcc-risk__get_judicial_documents` → 只能写“裁判文书 77 条（未取明细）”，**不得**写“多为原告身份、属正常维权”；如需该定性，必须先下钻 `mcp__qcc-risk__get_judicial_documents`（可按 `role` 取原告 / 被告分布）再下结论。
>
> **【关联风险先扫 · 防散弹枪 · 2026-06-27】** 凡本 SKILL 涉及"关联企业 / 关联方 / 实控人名下企业"风险排查，**先调** `mcp__qcc-risk__get_company_related_risk_scan`（企业关联 · 单锚）/ 对关键人 `mcp__qcc-executive__get_executive_related_risk_scan`（人关联 · 双锚）一次拿关联方风险面（有风险关联方 + 命中计数 · 单层预警）→ 仅对"有风险关联方"单点下钻；**禁止**先列对外投资 / 控制企业再逐个散弹枪 `mcp__qcc-risk__get_company_risk_scan`。
>
> 📌 **year 留空拿全量 · 禁逐年循环（防 year 散弹枪 · 2026-07-01）**：立案 / 裁判文书 / 开庭公告 / 法院公告等带 `year` 过滤参数的诉讼类工具，**取全量时 `year` 一律留空——接口在 year 缺省时即一次返回全部年份**；**严禁为“覆盖多年”而逐年（2024、2023 … 直至成立年）循环调用同一工具**（实测曾逐年一直调到 1976、单次运行 60+ 次冗余调用）。需要按年做趋势分桶时，基于“留空一次拿回的全量列表”在报告侧自行分桶；`role` / `notice_type` 等其他过滤参数同理，取全量时留空；仅当明确限定某一年 / 区间时才传 `year`。qcc-history / qcc-executive 的同名历史 / 个人诉讼工具同理，不逐年循环。

---


## 📖 QCC MCP 术语对照表（强制工具映射）

> **使用约定**：本表列出 SKILL 内业务简写与企查查 MCP 工具的精确映射。AI 执行本 SKILL 时遇到下表"业务简写"列的词汇，**必须调用对应"MCP 工具"列**，禁止使用 web search 或自由文本推测替代。完整规范见 [QCC-MCP-TERMINOLOGY.md](../../QCC-MCP-TERMINOLOGY.md)。

| 业务简写 | 规范全名 | 企查查 MCP 工具 |
| --- | --- | --- |
| 失信 | 失信被执行人 | `mcp__qcc-risk__get_dishonest_info` |
| 被执行 | 被执行人 / 判决债务人 | `mcp__qcc-risk__get_judgment_debtor_info` |
| 限高 | 限制高消费 | `mcp__qcc-risk__get_high_consumption_restriction` |
| 限出境 / 限境 | 限制出境 | `mcp__qcc-risk__get_exit_restriction` |
| 终本 | 终结本次执行案件 | `mcp__qcc-risk__get_terminated_cases` |
| 破产 / 重整 | 破产重整 | `mcp__qcc-risk__get_bankruptcy_reorganization` |
| 经营异常 | 经营异常 | `mcp__qcc-risk__get_business_exception` |
| 严重违法 | 严重违法失信 | `mcp__qcc-risk__get_serious_violation` |
| 行政处罚 / 重大处罚 | 行政处罚 | `mcp__qcc-risk__get_administrative_penalty` |
| 股权冻结 | 股权冻结 | `mcp__qcc-risk__get_equity_freeze` |
| 股权出质 | 股权出质 | `mcp__qcc-risk__get_equity_pledge_info` |
| 欠税 | 欠税公告 | `mcp__qcc-risk__get_tax_arrears_notice` |
| 税务异常 / 税务违法 | 税务异常 / 税收违法 | `mcp__qcc-risk__get_tax_abnormal` / `mcp__qcc-risk__get_tax_violation` |
| 受益所有人 / UBO | 受益所有人 | `mcp__qcc-company__get_beneficial_owners` |
| 实控人 / 实际控制人 | 实际控制人 | `mcp__qcc-company__get_actual_controller` |
| 主要人员 / 董监高 | 主要人员 | `mcp__qcc-company__get_key_personnel` |
| 抽查检查 / 双随机 | 双随机抽查 | `mcp__qcc-operation__get_random_check` |
| 吊销 | （登记状态字段判断）| 调 `mcp__qcc-company__get_company_registration_info` 取"登记状态" |
| 资不抵债 | （资产负债率字段判断）| 调 `mcp__qcc-company__get_financial_data` 判断负债率 > 100% |

---

# 新供应商快速筛选 · 企查查 MCP V2.0 增强版

## SKILL 定位

本 SKILL 是采购寻源的"筛子"——在进入正式"准入评估"或"IC Memo 级"深度尽调前，快速从候选池中排除"显然不合格"的主体。V2.0 `mcp__qcc-operation__get_random_check` 仅用于核对已完成抽查记录；接口不返回抽查结果，记录存在或不存在均不能证明合规 / 违规，因此不参与综合评分。

## MCP 依赖

- 必选：`qcc-company` / `qcc-risk`
- 强烈建议：`qcc-operation`（含 `mcp__qcc-operation__get_random_check`）

## 工作流

### 维度一：9 项核心红线快筛

对每家候选供应商并行执行：

| 序号 | 工具 | 红线判定 |
|------|------|---------|
| 1 | `mcp__qcc-risk__get_dishonest_info` | 当前失信 > 0 → 🔴 直接出局 |
| 2 | `mcp__qcc-risk__get_high_consumption_restriction` | 当前限高 > 0 → 🔴 出局 |
| 3 | `mcp__qcc-risk__get_judgment_debtor_info` | 当前被执行 > 0 → 🔴 出局 |
| 4 | `mcp__qcc-risk__get_business_exception` | 当前经营异常 > 0 → 🔴 出局 |
| 5 | `mcp__qcc-risk__get_bankruptcy_reorganization` | 已进入破产程序 → 🔴 出局 |
| 6 | `mcp__qcc-risk__get_equity_freeze` | 股权冻结 > 0 → 🔴 出局 |
| 7 | `mcp__qcc-company__get_company_registration_info` | 登记状态吊销 / 注销 → 🔴 出局 |
| 8 | `mcp__qcc-risk__get_tax_arrears_notice` | 欠税 > 0 → 🔴 出局 |
| 9 | `mcp__qcc-risk__get_serious_violation` | 严重违法失信名单 → 🔴 出局 |

### 维度二：综合合规评分（未被红线排除的）

对通过红线的候选者计算综合评分：

| 指标 | 权重 | 评分逻辑 |
|------|------|---------|
| 纳税信用等级 | 31.25% | A=100 / B=70 / C=40 / D=0 |
| 海关信用等级 | 18.75% | 高级 AEO=100 / 一般认证=80 / 备案=50 |
| 参保人数 | 12.5% | 100+=100 / 50-100=80 / 10-50=60 / <10=30 |
| 资质证书数量 | 12.5% | >10=100 / 5-10=80 / 1-5=50 / 0=0 |
| 招投标活跃度 | 12.5% | 高=100 / 中=70 / 低=30 |
| 荣誉信息 | 12.5% | 国家级=100 / 省级=70 / 市级=40 / 无=20 |

**综合评分 = Σ(指标得分 × 权重)**

> 权重变更：删除无数据依据的双随机 20% 评分后，将其余 6 项原权重按 80% 基数等比例归一化至 100%，保持原相对重要性。双随机记录只作事实展示，不计分。

### 维度三：短名单生成

1. 排除所有 🔴 红线触发者
2. 对剩余候选按综合评分降序排列
3. 返回 Top N（默认 Top 5）

## 报告输出格式（严格填空骨架 · 模型只填值、不造结构）

> **使用约定**：以下是新供应商筛选报告的**完整骨架**——标题层级、表头与列、免责声明**全部固定**，模型只把 `{}` 占位替换为工具返回值，**禁止新增 / 删除章节、禁止改表列、禁止虚构接口未返回的列或分类**。各章数据来源见每节标注（业务语言，报告内不写工具代码名）。本 SKILL 为「快速筛选」，不做股权穿透 / UBO / 财务深度核验——不要凭空补出工作流未覆盖的章节或数字。
> **填写纪律（务必遵守，对齐本 SKILL 已有铁律）**：① 风险维度一律**先扫后钻**——先看风险面分诊计数，仅对 `count>0` 维度下钻取明细；②【定性必须有下钻证据】对任一风险维度给定性（如"多为原告 / 属正常维权 / 轻微瑕疵"）前必须已下钻该维度明细，否则只写计数 +「（未取明细）」，禁凭计数或印象定性；③ 关联风险**单层预警**——只到一跳关联方风险面，不对返回的关联方再自动逐个穿透扫描；④ 风险结论**只陈述客观事实**（命中维度 + 计数 / 明细），**不替客户判定"能不能合作 / 可不可准入"**，准入档位仅按本 SKILL 既定红线 + 评分规则机械给出；⑤ 数值一律「数据零推算」——只引用接口原始 / 聚合数字，禁自行加总 / 相减 / 加权 / 相乘 / 估算，综合评分按既定权重逐项计算后给出、不另造数字，接口未返回字段写「未披露 / 本次未核验」、不编造；⑥ 简称 / 品牌名先实体锚定确定唯一主体（取唯一完整登记名 + 统一社会信用代码）后再筛。

```markdown
# 新供应商快速筛选 · 候选池筛查底稿

**筛选批次：** {本批候选供应商 N 家}
**报告生成：** YYYY-MM-DD HH:MM:SS
**审计留档编号：** NVS-{批次日期 YYYYMMDD}-{序号}
**筛选结论：** 入选短名单 {M} 家 · 红线出局 {K} 家 · 待定 {J} 家

---

## 执行摘要

> **一句话结论：** {本批 N 家候选中，K 家触红线出局、M 家进入短名单，Top1 为谁、推荐进入下一步（准入评估 / IC Memo）的是哪几家}

| 排名 | 供应商完整登记名 | 红线 | 综合评分 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | {} | {无 / 触线出局} | {分} | {入选短名单 / 出局 / 待定} |
| … | {按综合评分降序，红线出局者列末并标注} | | | |

**建议行动：** 1. … 2. … 3. …

---

## 1 筛选结论 · 决策摘要

{本批筛选了几家、几家触红线出局、几家进入短名单、Top N 是谁、建议哪几家进入下一步深度核验，3-5 句业务语言；不替客户拍板最终采购}

## 2 筛选方法与数据来源

| 维度 | 数据来源 | 取数方式 |
| --- | --- | --- |
| 红线快筛 | 企查查工商登记数据 + 企查查风险信息数据 | {先扫风险面分诊 + 命中维度下钻 / 登记状态字段判断} |
| 合规评分 | 企查查经营信息数据（纳税信用 / 海关信用 / 资质 / 荣誉 / 招投标 / 参保） | {按既定权重逐项取值计分} |
| 双随机抽查记录 | 企查查经营信息数据 | {仅核对计划、任务、类型、机关和完成日期，不计分} |
| 关联风险（如涉） | 企查查关联风险数据 | {一跳关联方风险面 · 单层预警} |

> 数据时效性：MCP 返回为查询时点快照，工商变更存在登记滞后（约 3-12 个月）；本筛选为寻源期初筛，不替代后续准入评估 / 尽调。

## 3 红线快筛结果（9 项核心红线 · 每家一行）

> 先扫后钻：先取各家风险面分诊计数，仅对 `count>0` 的红线维度下钻取明细；`count=0` 直接判「无」，不再调该维度。任一红线触发即出局。

| 供应商 | 失信 | 限高 | 被执行 | 经营异常 | 破产 | 股权冻结 | 登记状态 | 欠税 | 严重违法 | 红线判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| {完整登记名} | {无 / N 条} | {无 / N 条} | {无 / N 条} | {无 / N 条} | {无 / 已进入} | {无 / N 条} | {存续 / 吊销 / 注销} | {无 / N 条} | {无 / 在册} | {通过 / 触线出局} |


## 4 综合合规评分与双随机记录（仅未被红线排除者）

### 4.1 六维综合评分

| 供应商 | 纳税信用(31.25%) | 海关信用(18.75%) | 参保人数(12.5%) | 资质证书(12.5%) | 招投标活跃度(12.5%) | 荣誉信息(12.5%) | 综合评分 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| {完整登记名} | {A/B/C/D→分} | {AEO/认证/备案→分} | {人数→分} | {数量→分} | {高/中/低→分} | {国/省/市/无→分} | {Σ加权} |

### 4.2 双随机抽查记录（不计分）

| 完成日期 | 抽查机关 | 抽查类型 | 计划名称 | 任务名称 |
| --- | --- | --- | --- | --- |
| YYYY-MM-DD | {} | {} | {} | {} |

> 接口不返回抽查结果，不得补写“未发现问题 / 已整改 / 发现问题”，不得根据记录数量、有无记录调整综合评分或短名单排序。


## 5 关联风险预警（单层 · 如涉）

| 供应商 | 一跳关联方有无风险 | 命中维度计数 |
| --- | --- | --- |
| {完整登记名} | {有 / 无 / 本次未核验} | {} |

> 单层预警终点，不对返回的关联方再自动逐个穿透扫描；本节为可选补充，未触发关联排查需求时写「本次未核验」。

## 6 短名单与下一步建议

| 排名 | 供应商完整登记名 | 综合评分 | 入选理由（客观） | 建议下一步 |
| --- | --- | --- | --- | --- |
| 1 | {} | {分} | {无红线 + 关键评分项表现，客观陈述} | {准入评估 / IC Memo 深度尽调 / 补充资料} |
| … | {Top N，默认 Top 5；低于 --threshold 不入} | | | |

**出局清单：** {触红线出局的供应商 + 触发的具体红线维度，逐家写明}

---

## 数据来源与免责声明

**数据来源：** 本报告全部数据由企查查 MCP 实时返回（上游为国家市场监督管理总局及省 / 市市场监管、数据局公示数据），采集时间 YYYY-MM-DD HH:MM:SS。

**免责声明：**
1. 本报告为采购寻源阶段的**快速初筛**，仅基于公开工商 / 司法 / 经营公示数据，不含股权穿透、受益所有人识别、财务偿债深度核验——入选短名单不等于通过准入，须由「供应商准入评估」/「IC Memo 级尽调」进一步核验。
2. 数据为查询时点快照，存在工商变更登记滞后；涉及近期股权 / 资质变动的主体须以最新工商变更确认件为准。
3. 风险结论为客观事实陈述（命中维度 + 计数 / 明细），不构成最终采购决策意见；准入与否由采购方结合商务、技术、质量综合判定。
```

> **章节 ↔ 工具绑定**：执行摘要 ← 全维度汇总；§3 红线快筛 ← `mcp__qcc-risk__get_company_risk_scan` 先扫 + 命中维度原子下钻（`mcp__qcc-risk__get_dishonest_info` / `mcp__qcc-risk__get_high_consumption_restriction` / `mcp__qcc-risk__get_judgment_debtor_info` / `mcp__qcc-risk__get_business_exception` / `mcp__qcc-risk__get_bankruptcy_reorganization` / `mcp__qcc-risk__get_equity_freeze` / `mcp__qcc-risk__get_tax_arrears_notice` / `mcp__qcc-risk__get_serious_violation`）+ `mcp__qcc-company__get_company_registration_info`（登记状态）；§4.1 合规评分 ← `mcp__qcc-operation__get_credit_evaluation`（纳税信用）/ `mcp__qcc-operation__get_import_export_credit`（海关信用）/ `mcp__qcc-operation__get_qualifications`（资质）/ `mcp__qcc-operation__get_honor_info`（荣誉）/ `mcp__qcc-operation__get_bidding_info`（招投标）+ 工商参保人数，§4.2 双随机记录 ← `mcp__qcc-operation__get_random_check`（仅事实记录、不计分）；§5 关联预警 ← `mcp__qcc-risk__get_company_related_risk_scan`（单层）；§6 短名单 ← §3+§4.1 综合排序。

## 参数

- `--top N`：返回前 N 名，默认 5
- `--threshold <score>`：综合评分阈值，低于此值不入短名单
- `--format md|docx|pptx`：输出格式

---

**SKILL 版本**：v2.0.1 | **所需 Server**：qcc-company / qcc-risk（必选）、qcc-operation（强烈建议）


---

## 报告输出纪律（内部规则 · 严禁出现在最终报告中）

1. **一律业务语言**：报告正文、备注、数据来源说明中不得出现 MCP 工具代码名（`get_xxx` / `mcp__qcc-xxx`）、server 名（qcc-company 等）、schema / manifest / 字段名等技术词；数据来源统一用业务表述（如"企查查工商登记数据 / 企查查风险信息数据 / 企查查财务数据"）。"企查查 MCP"作为对外产品名仅允许出现在「数据来源」固定句式中。
2. **禁止内部用语**：SKILL / SKILL.md / V1.0 / V2.0 / 增强版 / 新能力 / 维度编号 / 评级引擎规则等开发概念不得出现在报告中；「Decision Pack」一律写「决策摘要」。
3. **禁止执行过程独白**：不输出"我将按照…/第一步获取…/已锁定主体/接下来…"等过程描述，直接输出报告正文。
4. **禁止运行时状态泄漏**：积分余额、配额、调用受限、超时重试、在线体验版本等不得写入报告；某维度数据未获取时统一写"本次未核验 / 未发现公开记录"。
5. **数据零推算**：只引用工具返回的原始数字；禁止自行加总、相减、加权、估算（含"推算 / 估算值"字样）；工具未返回的字段留空或写"未披露"，不得编造。
6. 本节及全部内部执行规则只约束 AI 行为，严禁以任何形式抄入报告。
<!-- QCC_ONLINE_EXPERIENCE_PLAN_V1
{"version":1,"steps":[{"id":"supplier-entity","label":"确认供应商主体","cap":"主体识别","capabilityKeys":["entity"],"completeOn":"route_match"},{"id":"supplier-registration","label":"核验准入信息","cap":"工商数据","capabilityKeys":["registration"]},{"id":"supplier-risk","label":"扫描准入风险","cap":"风险数据","capabilityKeys":["risk"]}]}
QCC_ONLINE_EXPERIENCE_PLAN_V1_END -->
